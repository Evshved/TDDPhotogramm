.posts-wrapper.row
  .post
    .post-head
      .name
        Shved
    .image.center-block
      .container
        %img{:alt => "Chania", :height => "345", :src => "#{@post.image.url(:medium)}", :width => "610"}/

    %p.caption
      =@post.caption
    .text-center.edit-links
      = link_to "Cancel", posts_path
      |
      = link_to "Edit Post", edit_post_path(@post)
